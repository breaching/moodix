# Application Mode
# Set to 'development' for dev mode (auto-reload, debug info)
# Set to 'production' for production deployment (optimized, secure)
FLASK_ENV=development

# Login credentials
# Generate password hash using: python hash_password.py your_password
APP_USERNAME=admin
APP_PASSWORD_HASH=<generate_with_hash_password.py>

# Optional: Secret key for Flask sessions
# If not set, a random one will be generated on each startup
# IMPORTANT: Use a strong random key in production!
SECRET_KEY=
